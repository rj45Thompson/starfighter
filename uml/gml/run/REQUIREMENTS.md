<!-- generated by gml; every line below is derived from starblast.json and trace.jsonl.
     Do not edit by hand: the next run overwrites it. Fix the model, not the report. -->

# starblast — requirements

5 epics · 29 use cases · 0 steps · 30 state literals · 0 open questions · 102 relations

Status: **HAVE** built and cited · **PARTIAL** built, story not fully true · **ABSENT** proven not present · **INFERRED** the chain needs it and nobody wrote it.

## E1 — Arrive and fly

> *You open the page. A world exists, you are in a ship in it, and the controls answer. Nothing else in the game can happen until this does.*

### UC-E1-1 — The page builds a world to fly in

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the page is open and nothing has started
- **Postcondition:** a world exists to fly in
- **Implemented by:** `renderer`, `scene`, `EXPOSURE`

Opening the page constructs a WebGL renderer and a scene. Until this exists there is no space, no ship, nothing to look at.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] with WebGL unavailable the page reports it rather than showing a blank canvas
- [ ] the renderer is constructed exactly once per page load

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E1-2 — The game gives the pilot a ship

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a world exists to fly in
- **Postcondition:** the pilot has a ship in that world
- **Implemented by:** `makeShip`, `SHIP_TEX`, `SHIP_R`

A ship is built for the player and placed in the world with a hull, a texture and a collision radius.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] ships[0] exists with role 'player' after load
- [ ] the ship has a non-zero collision radius, so it can be hit

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E1-3 — The simulation advances every frame

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot has a ship in that world
- **Postcondition:** the simulation is stepping
- **Implemented by:** `frame`, `step`

A frame loop runs, clamps the timestep so a stalled tab cannot teleport the world, and steps the simulation.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a frame gap longer than 50ms is clamped rather than integrated whole
- [ ] pausing and resuming the tab does not move the ship

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E1-4 — The pilot's controls move the ship

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the simulation is stepping
- **Postcondition:** the controls answer the pilot
- **Implemented by:** `THRUST`, `DRAG`, `VMAX`, `YAW_RATE`, `PITCH_RATE`

Thrust, yaw and pitch answer the keyboard, bounded by drag and a maximum speed, so the ship is flyable rather than merely present.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] holding thrust raises speed until it plateaus at VMAX, not beyond
- [ ] releasing thrust decays speed rather than stopping dead

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E1-5 — The pilot can see where the ship is going

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the controls answer the pilot
- **Postcondition:** the pilot is flying and in control
- **Implemented by:** `CAM_BACK`, `CAM_AHEAD`, `CAM_LAG`, `CAM_UP`

A chase camera trails the ship, leads it slightly, and lags rather than snapping, so the pilot has a usable frame of reference.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] the camera sits behind and above the ship at rest
- [ ] a hard turn moves the camera smoothly, with no instantaneous jump

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

## E2 — Mine the belt

> *You fly a small ship at some rocks. You shoot one, it breaks, and the pieces are worth something. You scoop them up and the bar at the bottom fills. When it fills, you are owed something.*

### UC-E2-1 — The system populates a belt with rocks

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot is flying and in control
- **Postcondition:** a belt of rocks exists
- **Implemented by:** `makeMiningBelt`, `spawnAsteroid`, `AST_MAX`, `AST_KEEP`, `ROCK_POOLS`

A mining belt is seeded with instanced asteroids, capped so the field is dense without becoming thousands of draw calls.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] the field holds rocks up to AST_MAX and never more
- [ ] rocks are drawn from instanced pools, not one mesh each

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E2-2 — The system finds the rocks near the pilot cheaply

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a belt of rocks exists
- **Postcondition:** a rock is within reach
- **Implemented by:** `AST_CELL`, `AST_GRID_EVERY`

A spatial hash rebuilt every few frames answers 'what is near me' without scanning the field, which is what makes a belt of thousands testable every frame.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] collision testing does not scan every rock each frame
- [ ] a rock marked gone is skipped by the near-query rather than returned

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E2-3 — The pilot breaks a rock apart

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a rock is within reach
- **Postcondition:** gems are loose in the world
- **Implemented by:** `destroyAsteroid`, `AST_SPLIT_MIN`, `AST_SPLIT_MAX`, `AST_CHILD_FRAC`, `AST_SPLIT_KICK`

A rock that takes enough damage is destroyed, splitting into smaller children carrying a fraction of its size and a kick outward.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a destroyed rock above the no-split size yields between AST_SPLIT_MIN and AST_SPLIT_MAX children
- [ ] the children move apart rather than sitting inside each other

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E2-4 — Broken rock leaves gems in the world

- **Actor:** —
- **Status:** HAVE
- **Precondition:** gems are loose in the world
- **Postcondition:** a gem can be picked up
- **Implemented by:** `spawnGem`, `GEM_CREDIT_PER_SCALE`, `GEM_LIFE`

The pieces spawn as physical gem pickups whose credit value scales with the size of the rock they came from, and which expire if nobody takes them.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a bigger rock yields gems worth more credits than a smaller one
- [ ] an uncollected gem disappears after GEM_LIFE seconds

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E2-5 — The pilot scoops a gem

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a gem can be picked up
- **Postcondition:** gem credits are in the hold
- **Implemented by:** `magnetFor`, `GEM_MAGNET`, `GEM_PICK_R`, `GEM_GRAB_R`

Flying near a gem collects it, with a magnet radius that widens as the ship is upgraded, so collection is a flight skill rather than a pixel-hunt.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a gem inside the magnet radius moves toward the ship without an exact pass-over
- [ ] the magnet radius grows when the relevant upgrade is bought

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E2-6 — Collected credits bank an upgrade point

- **Actor:** —
- **Status:** HAVE
- **Precondition:** gem credits are in the hold
- **Postcondition:** the pilot is owed at least one upgrade point
- **Implemented by:** `gemBarAdd`, `GEM_BAR_MAX`, `GEM_BAR_RATE`, `GEM_PTS_CAP`

Gem credits fill a bar; each time it fills it converts into one banked upgrade point, up to a cap.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] GEM_BAR_MAX credits collected converts to exactly one banked point
- [ ] only the player's ship banks points; an AI ship collecting gems banks none
- [ ] banked points stop accumulating at GEM_PTS_CAP

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

## E3 — Spend the point, or save for the hull

> *You have a point. Put it into guns, shields or engines and get a little better now; or hold out, max everything, and trade the whole ship for a bigger frame and lose every upgrade you bought. That is the only real decision in the game.*

### UC-E3-1 — The pilot spends a point on one of eight stats

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot is owed at least one upgrade point
- **Postcondition:** at least one stat is above zero
- **Implemented by:** `spendStat`, `STAT_ORDER`, `STAT_GAIN`, `STAT_MAX`

A banked point goes into one of eight named stats, in flight, each capped so no single stat absorbs the whole run.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] spending on an unknown stat name is refused with the list of real ones
- [ ] a stat already at STAT_MAX refuses further points rather than consuming them

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E3-2 — Each point visibly changes how the ship performs

- **Actor:** —
- **Status:** HAVE
- **Precondition:** at least one stat is above zero
- **Postcondition:** the ship is measurably better than it was
- **Implemented by:** `statMult`

A stat is a multiplier the simulation actually reads, so a point spent is felt rather than merely displayed.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] raising speed increases the ship's achieved velocity in flight
- [ ] a stat at zero leaves its multiplier at exactly 1.0

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E3-3 — Maxing every stat arms the trade-in

- **Actor:** —
- **Status:** HAVE
- **Precondition:** at least one stat is above zero
- **Postcondition:** every stat is at its cap
- **Implemented by:** `tierUpReady`, `TIER_UP`, `STAT_MAX`

Only when all eight stats are at their cap, with a further point banked, does the ship become eligible for the next hull.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] with seven stats maxed the tier-up is not offered
- [ ] with eight maxed and no banked point the tier-up is not offered

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E3-4 — The game names the hull that comes next

- **Actor:** —
- **Status:** HAVE
- **Precondition:** every stat is at its cap
- **Postcondition:** a next hull is on offer
- **Implemented by:** `nextTierHull`, `hullTierIdx`, `HULL_ORDER_ORIGINAL`

The ladder of hulls is a fixed order, and the ship's current class determines exactly which one is on offer.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a ship at the top of the ladder is offered nothing
- [ ] a generated hull is placed at its size tier's rank rather than off the ladder

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E3-5 — Taking the new hull costs every upgrade bought

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a next hull is on offer
- **Postcondition:** the ship is flying a new frame
- **Implemented by:** `takeTierUp`

Accepting the trade-in swaps the frame and resets all eight stats to zero, which is what makes holding out a real decision rather than a free reward.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] after a tier-up every stat reads zero
- [ ] refusing the tier-up leaves the current hull and stats untouched

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E3-6 — The traded-up frame outperforms the one it replaced

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the ship is flying a new frame
- **Postcondition:** the ship is measurably better than it was
- **Implemented by:** `HULLS`, `shieldMaxFor`

A hull class carries its own capacities, so the frame taken in the trade is materially better than the one given up even with every stat reset to zero. This is what makes the trade-in a decision rather than a punishment, and it is the beat that joins the tier-up arm back to the rest of the story.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a ship one tier up, with all stats at zero, has a higher base shield capacity than a maxed ship of the tier below
- [ ] each entry in HULLS declares its own capacities rather than inheriting one shared set
- [ ] the balance law the table states -- bigger hold means thinner armour and lower speed -- holds between adjacent tiers, so the trade is a trade and not a strict gain

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

## E4 — Fight, die, and lose the cargo

> *Your gun costs energy, so you cannot hold the trigger forever. Your shields soak the first hits and come back if you break away. When you lose, your gems spill for anyone to take and you start again small.*

### UC-E4-1 — Firing costs energy the pilot cannot spend forever

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot is flying and in control
- **Postcondition:** the ship can fire
- **Implemented by:** `capMaxFor`, `LASER_CAP_MAX`, `LASER_CAP_REGEN`, `LASER_CAP_PER_SHOT`

Each shot draws from a capacitor that refills over time, so sustained fire is bounded by a resource rather than by a cooldown alone.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] with the capacitor empty the ship cannot fire
- [ ] breaking off refills the capacitor at LASER_CAP_REGEN per second
- [ ] raising energyCap raises the ceiling the capacitor refills to

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-2 — A shot travels a bounded distance

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the ship can fire
- **Postcondition:** a shot is travelling
- **Implemented by:** `BULLET_SPEED`, `BULLET_LIFE`, `MUZZLE_OFF`

A bullet leaves the muzzle at a finite speed and expires after a finite life, which is what gives the weapon a range and makes closing the distance a decision.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a bullet expires after BULLET_LIFE seconds rather than travelling forever
- [ ] a target beyond BULLET_SPEED times BULLET_LIFE cannot be reached by a direct shot

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-3 — A shot that connects damages what it hit

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a shot is travelling
- **Postcondition:** damage is being exchanged
- **Implemented by:** `BULLET_DMG`, `BULLET_R`, `FIRE_CD`

Contact applies damage, and the gun cannot be fired again until its cooldown has passed, so damage over time is bounded by the weapon rather than by the key.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a bullet passing within BULLET_R of a ship registers a hit
- [ ] firing faster than FIRE_CD apart is refused

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-4 — Shields take the hit before the hull does

- **Actor:** —
- **Status:** HAVE
- **Precondition:** damage is being exchanged
- **Postcondition:** a shield arc is down
- **Implemented by:** `drainShield`, `shieldArcMaxFor`

Damage is absorbed by a directional shield arc, each arc carrying half the ship's total shield, before any of it reaches the hull.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a hit from ahead drains the forward arc and leaves the aft arc untouched
- [ ] hull integrity does not fall while the struck arc still has charge

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-5 — A ship that runs out of hull dies

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a shield arc is down
- **Postcondition:** the pilot's ship is destroyed
- **Implemented by:** `killShip`, `killByShip`

When damage exhausts the hull the ship is destroyed, removed from the fight, and the kill is attributed to whoever landed it.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a destroyed ship stops being a valid target
- [ ] the killer is credited, so a kill is attributable

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-6 — Death spills the cargo for anyone to take

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot's ship is destroyed
- **Postcondition:** the hold is on the floor
- **Implemented by:** `spillGems`

The credits banked into the hold are dropped into the world on death, so a long uninterrupted run is worth more than the sum of its minutes.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a pilot dying with credits leaves collectable gems at the wreck
- [ ] a pilot dying with nothing spills nothing

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E4-7 — The pilot comes back small after a pause

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the hold is on the floor
- **Postcondition:** the pilot is flying again after dying
- **Implemented by:** `RESPAWN_DELAY`

After a delay the pilot respawns without the hull they lost, which is the cost that makes the fight matter.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] respawn does not happen before RESPAWN_DELAY seconds have passed
- [ ] the respawned ship is not the tier that died

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

## E5 — The run ends and is remembered

> *The session stops, and what you did survives it: the score, the ship you were flying, the state that has to be there when you come back.*

### UC-E5-1 — The game gathers what is worth keeping

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the pilot is flying again after dying
- **Postcondition:** a snapshot of the run exists
- **Implemented by:** `gatherSaveState`

The player's ship, progress and standing are collected into one versioned snapshot.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] the snapshot carries a version field, so an old save is recognisable as old
- [ ] with no player ship the snapshot is refused rather than written empty

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E5-2 — The snapshot is written where it can be found again

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a snapshot of the run exists
- **Postcondition:** the snapshot is in storage
- **Implemented by:** `SAVE_KEY`, `persist`

The snapshot is stored under a known key in browser storage, so it survives the page closing.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] closing and reopening the page finds the previous snapshot
- [ ] a snapshot written under the key replaces the previous one rather than accumulating

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E5-3 — The snapshot is kept current, not written only at the end

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the snapshot is in storage
- **Postcondition:** the stored snapshot is no more than one interval old
- **Implemented by:** `SAVE_INTERVAL_S`

Saving runs on a cadence during play, so an unclean exit costs at most one interval rather than the whole session.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a session ended without a clean exit still restores to within SAVE_INTERVAL_S of where it stopped

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E5-4 — A returning pilot gets their ship back

- **Actor:** —
- **Status:** HAVE
- **Precondition:** the stored snapshot is no more than one interval old
- **Postcondition:** a returning pilot has their ship back
- **Implemented by:** `applySaveState`

On load a valid snapshot of the right version is applied; an invalid or older one is declined rather than half-applied.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a snapshot of the wrong version is declined and the pilot starts fresh
- [ ] a restored pilot has the hull and stats they had when the snapshot was taken

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

### UC-E5-5 — The run's standing is a weighted total

- **Actor:** —
- **Status:** HAVE
- **Precondition:** a returning pilot has their ship back
- **Postcondition:** the run is over and what happened is on record
- **Implemented by:** `SCORE_W`

Standing combines credits and score under a stated weight rather than counting kills alone, so mining and fighting both register.

**Acceptance — each of these is an observation that would be false if this were not implemented**

- [ ] a run that only mines still produces a non-zero standing
- [ ] two runs with equal credits and different scores are ranked apart

*Support: 5/5 — grounded, precondition_met, postcondition_used, falsifiable, attributed. Rubric 100/100.*

