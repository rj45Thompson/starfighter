# AGI SALVAGE LEDGER — nothing gets lost

**Why this file exists** (user, 2026-07-09): *"i worked really hard on all the agi stuff I just don't want it to
be lost... we have essentially stuff all over. some might work some might not."* Work is lost when nobody can
find it — not when it's unported. This ledger is the finding mechanism: every pillar, every known implementation
across the 13-repo ecosystem, which one is CANONICAL, and where it is (or will be) proven inside the game.
**The space game is the proving ground, not a game** — a pillar counts as salvaged only when it has an in-game
test command a human can run.

Source basis: the user's 13-repo clone-and-read survey (2026-07-09) + this repo's own code + a local-disk
verification agent (landed 2026-07-09 — every path below is disk-verified). Update discipline: when a pillar's
canonical pick changes, note WHY here — never silently.

## Local disk map (verified — name-aliases are the #1 confusion source)
| GitHub name | Local truth |
|---|---|
| Warship4JuneHyperdineCrystals (TDRE canonical head) | **`D:\code\TDRE_fixed_5`** (its git remote IS that repo) |
| LLMExtractor ("Jobe") | **`D:\code\Jobe`** (remote = LLMExtractor.git); sibling `D:\code\JobeCodex` |
| combine-iso / combine-iso-agi-fleet | **ABSENT locally** — GitHub-only (only an arcWelderPro push remote). P1/P2/P7/P12 candidates need a clone before porting. |
| TDRE / tdre-cognitive-architecture / warship_arc3 / BorgBench / sober-ai-complete / arcWelderPro / correlation-anchor-theory | all present at `D:\code\<name>` |
| `kripke_vector*` files | **do not exist anywhere** — a remembered name, not a file |
| gravity-well repair | NOT in TDRE_fixed_5 — lives at **`D:\code\Tami\.opus-tools\agi_proto\chainer.py`** (`_gravity_well`, pose-graph least-squares, repair-not-prune) |

## Kripke census (all 6 local implementations, disk-verified)
| Where | What its worlds/□/◇ actually are | Portable? |
|---|---|---|
| TDRE lineage `src/tdre/kripke.py` (5 copies) | worlds=ARC grid states, R=DSL transform; □=rule holds on every training pair | grid-bound — idea only |
| sober-ai `substrate/seo/kripke_frame.py` | worlds=KB snapshots (current/+pending/−contested/historical/strict), □/◇ over (s,p,o) claims | **most portable** — the game's `kripke_mind.js` port matches this shape (minds-as-worlds variant) |
| Jobe `jobe/kripke.py` | load-time graph CLEANER: polarity→modality, prunes contradictions + subsumed hops | portable, complements knowledge.js |
| Jobe `jobe/runtime/kripke_memory.py` | labeled transition system: worlds=game states, R[action]; `ensures(w,p)` = actions that NECESSARILY cause p | **PORTED 2026-07-09** as `kripke_planner.js` — see ports-completed table |
| arcWelderPro `state/kripke_reason.py` | worlds=mechanic hypotheses | ARC-bound |
| TDRE_fixed_5 `experiments/kripke_triangulation.py` | Kripke gate VETOes sphere inheritance on exception worlds | idea feeds the game's gate design |

Iron rule, applying to every port: nothing may be handed to a mind that it claims to have learned; claims commit
only over-determined (≥2 edge-disjoint evidence) or the mind ABSTAINS; GIVEN vs LEARNED is always labeled.

## Legend
- ✅ **in-game** — ported, self-tested, live test command exists
- 🔶 **partial** — some of the pillar is in-game, gaps named
- 📦 **salvageable** — best implementation identified, not yet ported
- ⚠ **unverified** — exists but no reproduced result; port only with a fresh test
- 🪦 **retire** — superseded by a better implementation of the same idea (the work is *kept here by name*, the code stops being the reference)

## The pillar table

| Pillar | Canonical implementation | Status | In-game proof | Also-rans (kept by name, not lost) |
|---|---|---|---|---|
| P1 Embodiment | combine-iso `agi_proto/phys_vec.js` (torque recovery, mutation-tested) | 📦 local:pending | — (candidate: a "decode your own thrusters" pilot mode) | starfighter's own flight model is HANDED physics, not decoded — not a P1 claim |
| P2 Navigation | combine-iso survey entry (orphaned) | 📦 local:pending | — | game's A3 learned nav-gain (`s.navGain` adapters) is a live partial |
| P3 Spatial grounding | game `vision.js` retinas + A4 sensor-noise beliefs (`senseTarget`) | ✅ (live) | fly with GROUNDED on — aim tracks beliefs not truth | combine-iso spatial entries |
| P4 Economy | game organic economy (`econTick`, no-cheat priced trades) | ✅ (live) | `market` / SR-M13 shocks verified 2026-07-08 | — |
| P5 Communication | game chatter/radio + emergent-signal glyphs (W16 σ-codes) | 🔶 | RADIO tab; audited code = combine-iso world_vec/lang_vec | second implementation flagged `redundant` in survey — pick after local check |
| P6 Theory of mind | game INH/DISPOS + roster rel matrix + **`tom_test.js` falsifiable test (2026-07-09)** | ✅ (has a falsifiable test now) | `tom` - two measures, both baseline-gated: A2 target-ID (nose-alignment vs random baseline) + next-action prediction (observable-state table vs majority AND persistence baselines) | the ledger's own "needs a real falsifiable test" flag is CLEARED - the test can and does report FAILURE honestly (a structureless window scores not-better-than-base) |
| P7 Open-ended goal discovery | game A11 `discoverTarget` (type-blind buckets) | 🔶 wired, partial | live: pilots pick own targets | combine-iso M8 curiosity→pen→promote loop (📦) |
| P8 Reasoning & logic | **TDRE lineage head** (Warship4June = TDRE_fixed_5 local) `pillar_router.py` 7 strategies; game holds two ports already | 🔶 | `deliberate` (How/What/Why/When 4-axis + convergence) · `kripke <s> <r> <o>` (NEW 2026-07-09: modal □/◇/✗ over one-world-per-mind) | TDRE `kripke.py` (ARC-grid-bound — idea ported, code not portable); sober-ai KripkeFrame (needs pre-built checkers) — both 🪦 for the game's purposes, named here |
| P9 Memory & knowledge representation | game `knowledge.js` two-tier provenance store (shared/private by construction) | ✅ | `observatory` (fullscreen graph + live commit feed) | survey found 4 independent KG builds — the OTHER three 🪦 to this one for game use; local agent to name them explicitly |
| P10 Verification & anti-hallucination | game iron-rule stack: `centroid_mind.js` gate (margin + ≥2 edge-disjoint objects) + `deliberate.js` verifyChain + 0-fab rumors | ✅ | `mind eval` — held-out: raw 47.1% vs 23.5% majority, **gated 100%/6 answered, 11 abstains** (2026-07-09, real AgentWorldBench) | sober-ai `sober_check`/Jobe `query_gate.py` (📦 — port the CHECK pattern as a second gate if local read shows a distinct mechanism) |
| P11 Benchmarking & evaluation | arcWelderPro blind honesty auditor | 📦 local:pending | game-side: every `* eval` command is the in-game analog | warship_arc3 Kaggle 0.23 (real, external); BorgBench (⚠ isolated) |
| P12 Orchestration & agency | combine-iso fleet orchestrator | 📦 (stays out of the game — it orchestrates *sessions*, not pilots) | n/a | agent_mail/agent_super (Tami .opus-tools) already serve this locally |
| P13 Theory | correlation-anchor-theory (markdown, no code) | 📦 by design | n/a | — |

## Ports completed into the game (the proving-ground record)
| Date | Port | From | In-game test | Result |
|---|---|---|---|---|
| 2026-07-07 | Two-tier knowledge store | ecosystem KG concept | `observatory`, persists across generations | live, 1,096 shared facts |
| 2026-07-07 | Deliberation engine (4-axis + geometry importance + seek) | TDRE concepts | `deliberate <planet>` | live; abstains honestly on gaps |
| 2026-07-09 | Centroid category mind (one-hot + centroid + gate) | llmextractor exp1 proven pattern (NOT the below-chance SVD/chain) | `mind eval / classify / learn` | gated 100% on answered, 11 abstains, 6/6 learned facts correct |
| 2026-07-09 | HF dataset training under iron rule | new | `mind eval` over `in_task` | see P10 row |
| 2026-07-09 | **Kripke diamond** (modal □/◇ over minds-as-worlds) | TDRE/sober-ai Kripke *idea* | `kripke` / `kripke <s> <r> <o>` + □/◇ glyphs in observatory walks | 6/6 self-test; live verdicts name exactly which minds hold a secret |
| 2026-07-09 | **sober_check gate** (3-check refuse-BEFORE-answer: grounding floor → identifier existence → prefix-stem coverage; NLI fallback honestly NOT ported - no model in-browser) | `sober-ai-complete\baseline_1\sober_ai_api.py:163`, source read verbatim first | `sober <question>` + pre-gate fronting every Observatory ask (two independent gates = system-level over-determination) | 8/8 self-test incl. two LIVE-caught adaptation fixes (identifier verdict must outrank the bare floor so fabricated names get NAMED; compound node ids must split before stemming or plain-word queries can't be covered by the facts that ground them); live: fake tool refused by name, real queries commit |
| 2026-07-09 | **contradiction ledger** (claims keyed (world,subject,predicate); functional predicates = one value per key; same-world conflicts QUARANTINE; the modal don't-collapse-worlds rule maps 1:1 onto the two-tier store; the (type,value) bucket trick ported) | `TDRE_fixed_5\TDRE\src\tdre\contradiction.py`, source read verbatim first | `contradictions` command; wraps GAME_KNOW.commit (live flagging); sober gate adds `refuse_contradicted`; centroid mind abstains on tainted labels (consult BEFORE the known-label shortcut - live-caught); Observatory ⚠-marks tainted edges | 10/10 self-test; **first live audit caught 3 REAL contradictions = a genuine bake bug** (idOf hashed numeric Json row-ids tiny → two rows collided onto one node, welding android+os labels) → bake position-salted, v1 facts purged via provenance-keyed migration, v2 re-ingested: 0 contradictions, and the mind's clean-data eval jumped raw 47→64%, gated 100% on 20 answered (was 6) |
| 2026-07-09 | **Observatory gravity-layout** (INTEGRATION, not a port - the first pillar made load-bearing on ANOTHER pillar): the Knowledge Observatory can now lay its graph out at the gravity-well's SOLVED coordinates instead of a golden-angle spiral, so every relation reads as a consistent DIRECTION and connected nodes cluster - the gravity well stops being an eval command and becomes the graph's actual geometry | consumes GWELL.positions()/report() (proven 71%/96% fact-recovery); knowledge_screen.js layout() gains a gravity branch with a pure `fitToCanvas` helper (uniform scale preserves solved angles), falls back to spiral for GWELL-unplaced nodes so nothing vanishes | the Observatory's `◇ LAYOUT: SPIRAL / ◉ GRAVITY` header toggle; header shows the live residual + relation-direction count; positions labeled INFERRED in the tip | 16/16 self-test (5 new gravity: in-box placement, geometry preserved - a square stays a square, relation directions consistent, null on empty, spiral fallback with no GWELL); live vs the real 746-node solve: gravity places all 97 drawn nodes, connected nodes measurably TIGHTER (avg edge 196px vs spiral 255px), modes differ, header residual immediate on toggle, contact-sheet PNG read with eyes (coherent clustered layout, not a spiral) |
| 2026-07-09 | **P6 ToM falsifiable test** (predict a mind's NEXT ACTION from strictly OBSERVABLE state only - mode/hull-band/visible-threat/role, never temperament or ray-weights or private beliefs; online transition table trained on the first half, scored on a held-out second half; TWO honest baselines - majority AND persistence - and the claim stands ONLY if it beats both; unseen basis buckets ABSTAIN; transition-only accuracy reported separately because that is where prediction is actually hard) | designed this tick (the ledger's own P6 row demanded a falsifiable test before any claim); complements A2's existing live target-ID tally | `tom [seconds]` / `tom report` - runs a scoring window over sensed ships, prints prediction accuracy vs both baselines + the honest verdict | 7/7 self-test (structured world beats both baselines 0.94 vs 0.35/0.45; **structureless world HONESTLY reports NOT-better-than-base-rate**; unseen-bucket abstain; observable-only basis proven - temperament never enters the key; sampler pairs consecutive observations, drops out-of-range ships; under-evidence abstain); live: basis read observable-only off a real ship (ORION), pure pipeline ran end-to-end on real snapshots, command dispatches both measures; ⚠ live-caught: `window.TOM` name-collided with A2's inline `const TOM` (renamed the module to `window.TOMTEST`), and an x40 sensor-scaffold got autosaved - both scrubbed, senseR restored to 95 and re-persisted |
| 2026-07-09 | **M8 growth loop** (curiosity → ASK → holding-pen quarantine → rule-gated PROMOTE → true-majority rule graduation → clean defeasible DEMOTE; the three adversarially-earned fault gates ported intact: internal consistency over exclusion clusters - never sibling head-counts; strict-majority-of-probed graduation; conformer/exception-cluster-only commits with lone TRUE exceptions honestly abstained; 4-state defeasible resolver + replayable audit ledger) | `combine-iso-agi-fleet\agi_proto\m8_growth.py` + `m8_graph.py` (repo CLONED this tick - was GitHub-only; note: `combine-iso` alone is not a repo, the fleet repo is), sources read verbatim first; honest mapping: members = ships on the player's REAL sensors, class = hull kind (GIVEN like the original's skeleton), properties = team/role fields read through the DISTANCE-NOISY sensor channel (values mutually exclude by construction) | `grow` / `growth` commands; promoted facts commit to GAME_KNOW's private 'growth' layer with full provenance (LEARNED); CONTRA-quarantined members excluded from tallies | 16/16 self-test (ALL ELEVEN of the original's pinned invariants 1:1: consensus, exception cluster, two singleton faults quarantined, lone true exception abstained, minority-rule block, true-majority block, held-out PRESUMED, override REFUTED, ledger replay identity, dominance-flip demotion + 5 game-adapter tests); **live: from 124 noisy reads over all 31 real ships it LEARNED the galaxy's actual spawn norms - freighter=>is_role_trader (support 16) and freighter=>is_team_squad (support 17) - 16 verified facts committed, 108 observations honestly quarantined**; scaffolded-run commits scrubbed (the player earns their own growth) |
| 2026-07-09 | **gravity-well repair** (relations as CONSERVED TRANSLATIONS: t[r] = mean edge delta; residual = mean constraint violation; AUTONOMOUS trigger - repair only when residual/scale runs high, the agent's own decision, reported; pose-graph least-squares pulls every node to the position most consistent with ALL its facts; REPAIR-NOT-PRUNE - edges never touched, only positions; anchor row pins uniqueness) | `Tami\.opus-tools\agi_proto\chainer.py` (pillar 12), source read verbatim first; honest mapping DECIDED before code: GAME_KNOW's ~1,600 real edges over 5 reused relations ARE the redundant constraint set (KPLAN frame rejected - too sparse early-game); adaptations labeled: numpy lstsq → deterministic iterative relaxation on the same objective, D=24 → D=2 (the game consumes a 2D semantic layout) | `gravity` / `repair` command: reports nodes/edges/relations, tainted edges EXCLUDED (CONTRA law), the autonomous trigger verdict, residual before→after, and fact-recovery-by-geometry on REAL store edges; positions exposed via GWELL.positions() for future Observatory layout | 10/10 self-test (the original's own 4-point AUDIT replicated on its own grid shape: multi-hop chaining, deep unstored compositions, no-chain ablation fails, autonomous repair restores chaining +0.34 mean > the original's 0.25 bar; + 6 game tests: taint exclusion, trigger+residual drop, repair-not-prune edge count, real fact recovery, INFERRED label, determinism); live vs the real store: 746 nodes / 1,601 edges / 5 relations, trigger fired autonomously (residual/scale 0.368), residual 51.7 → 0.05 in 57 sweeps (161ms), **pure geometry recovers 71% of 400 real stored edges (depth-1) and 96% of 400 real 2-hop compositions (both 0% pre-repair)** |
| 2026-07-09 | **pillar router + 4 solvers** (route-by-STRUCTURE never by family id; STRENGTH-then-TOTAL ranking so incidental vocabulary can't out-sum one genuine operator; ABSTAIN on a defining-operator tie - never a coin-flip; contradiction-first prepend on polarity conflict; PROPOSE-THEN-VERIFY over the ranked candidates with verifier-first solvers falling through on misroutes; ONE shared ledger) | `TDRE_fixed_5\TDRE\src\tdre\pillar_router.py` + `experiments\rootbench_solver.py` + the router's own pinned test file, all read verbatim first | `solve <question>` command; 4 game solvers answer ONLY what GAME_KNOW entails, quoting the facts: INHERITANCE (transitive isa, GIVEN vs INFERRED labeled), CONSTRAINT (functional-relation lookup - the exactly-one law CONTRA already enforces), COMPOSITION (relation-path folding, >=2 hops = composed), CENTRIC (shared-parent recovery); all four refuse on quarantined keys | 20/20 self-test (the router's NINE pinned tests replicated 1:1 incl. tie-abstain + incidental-vocabulary + sub-defining sum-tiebreak, + 11 game tests incl. verifier-first fall-through); live vs the real store: real functional lookup, real shared-task recovery, real 2-hop composition, honest abstain on unconnected entities, live tie-abstain, planted quarantine refused then scrubbed |
| 2026-07-09 | **modal planner** (Kripke labeled transition system: worlds = fingerprinted game states, one accessibility relation per ACTION; box vacuous-truth + `ensures` non-vacuous exclusion + BFS `reachable` ported exactly - the original's own 6 tests replicated 1:1 and passing; game layer adds the iron-rule support gate: 2+ agreeing observations = PROVEN, 1 = tentative, plus observed-route fallback labeled "possibility, not necessity" and two DISTINCT abstains - never-recorded state vs never-observed-true prop) | `Jobe\jobe\runtime\kripke_memory.py` + `tests\test_kripke_memory.py`, source read verbatim first | `plan <prop>` / `ensures` commands; transitions observed ONLY from real completed player actions (completeDock, buy/sell/refuel, hyperjump, killByShip) + a 'drift' watcher for uncommanded change (precedent: the original's configuration_bench "step" label); taint-aware (plan targets naming a CONTRA-quarantined entity refuse); persists in KPLAN_v1 across generations | 16/16 self-test (6 Jobe-pinned + 10 game-layer); live: REAL dock-button click → transition recorded, real buy/sell commands → `buy:ore`/`sell:ore` labels, real fly-out undock via drift, re-dock support=2, honest abstain with exact reason, taint refusal naming the entity, persistence round-trip; teleport-artifact frames + planted quarantines fully scrubbed after (the user's planner learns only THEIR play) |

## How we handle "stuff all over, some might work, some might not"
1. **Name everything here** — an implementation listed as 🪦 is not lost; it's credited and findable.
2. **One canonical per pillar** — chosen for portability + a reproduced result, not recency.
3. **Port = test command** — nothing counts as in-game until `something eval`-style proof exists.
4. **Unverified stays ⚠** — survey claims without reproduced numbers never get promoted by name-recognition.
5. **The game never absorbs orchestration** (P12) — proving ground for MINDS, not for session tooling.

## Next queue (INTEGRATION phase - the original port queue is DRAINED; now make the proven pillars load-bearing)
1. **Make ToM/growth load-bearing in GAMEPLAY** (not just eval commands): a pilot running the ToM predictor pre-empts a predicted hunter; a `grow`-learned rule pre-classifies an unscanned ship before the scan resolves. Measure a real before/after. (The Observatory gravity-layout - tick 9 - was the first "make a pillar load-bearing on another pillar" step; this is the gameplay version.)
2. combine-iso follow-ups now the fleet repo is local (`D:\code\combine-iso-agi-fleet`): M9 cooperative-proof (2 provenance-distinct attesters), M10 self-improvement bench — survey before queueing.
(Path corrections recorded: chainer's repair = module-level `repair()`, not `_gravity_well`; the combine-iso repo is `combine-iso-agi-fleet` — plain `combine-iso` does not exist.)
