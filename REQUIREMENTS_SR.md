# REQUIREMENTS_SR — Space Rangers 2 burndown → starfighter (three.js)

Mapping **Space Rangers 2: Rise of the Dominators**' core systems onto our game, plus the Tami
2D-topdown battle-presentation bring-over list. NO code here — inventory, milestones, checklists.
See `USE_CASES.md` for the actor/use-case (UML) view built on top of this same audit.

Ground truth audited 2026-07-07 against:
- `starfighter.html` help text + command surface (`runCmd`, line ~1638) and module internals
- `PILLARS.md` (the bench law — every SR feature we add must not poison a pillar's claim-path)
- `NOVEL_SOCIETY.md` (N1–N8 status: novel/graph/roster live; chatter live; N5 bench-only-and-inert-in-loop; N7 **verified live 2026-07-08** (50-scale perf pass, see SR-M8); N6 still **not started**)
- `SR2_MILESTONES.md` (space-layer M1–M11 landed, M12 stronghold landed per CFG/commands; A-line partial)
- `Tami/Assets/Docs/BattleRequirements.txt` (NOTE: lives under `Tami/Assets/Docs/`, not repo root)

Honesty rule inherited from PILLARS.md: mechanics only, original names/text/art (Dominators → **HEGEMON /
Iron Synod**; rangers → **Wardens**); nothing below claims a feature we don't have — MISSING means MISSING.

---

# SR BURNDOWN

Status legend: **HAVE** (full) · **HAVE-partial** (exists, thinner than SR2) · **MISSING**.
Game fit = how well the SR2 feature serves THIS game's identity (AGI-society sim + Tami tie-in), 1–5.

| # | SR2 feature | Our status | Fit |
|---|---|---|---|
| 1 | **Galaxy of many star systems, each a 2D system map** (30–70 systems, per-system top-down maps, stations, orbiting planets) | **HAVE-partial** — 3 systems as clusters inside ONE continuous 840u arena (`CFG.N_SYSTEMS:3`, line ~150); orthographic TOP-DOWN tactical view + `map`/`galaxy` commands. No per-system map screens, no starmap/territory overlay, no stations besides Ranger Command. | 4 |
| 2 | **Hyperjumps** (fuel/engine-range gated jumps between systems; hyperspace transit + arcade duels) | **MISSING — deliberately removed** ("hyperspace removed for balance", CFG line ~150/180). All travel is real-space. Fuel/refuel exists and would gate a future jump system. | 3 |
| 3 | **Living economy** (per-planet markets, dynamic prices, trade AI, legal/illegal goods) | **HAVE** — organic prices emerge from stock (PROD/CONS/PRICE_ELAST; buy raises, sell lowers — never scripted); AI traders arbitrage the same market; planet dev levels + wealth; contraband + customs scans + fencing at hostile worlds (M10). This IS pillar P4 (`econ_vec`). | 5 |
| 4 | **Rangers: independent AI peers, ranked** (dozens of NPC rangers trading/fighting/levelling; galaxy leaderboard you climb) | **HAVE-partial** — player RANKS ladder Recruit→Legend gating gear (M5, `rank`); AI pilots that genuinely trade/fight/upgrade/learn (the AGI substrate; novel-charactered, radio chatter), scaling live to 50+ concurrent under the war-escalation curve (N7/SR-M8 verified 2026-07-08). MISSING: a ranked leaderboard of player-vs-AI (SR-M14), cross-generation ranking. | 5 |
| 5 | **The war vs Dominators** (machine invaders take systems; liberation ops; military coordination; boss brains) | **HAVE** — HEGEMON escalation tiers Drone→Cruiser→Warlord (M1), contested/liberated systems, coalition defenders scrambled (M2), warScore tug-of-war + campaign win/lose (M7), Stronghold boss + `assault` (M12), `war`/`warmap` readouts. MISSING: named persistent boss minds; Hegemon does not adapt across campaigns. | 5 |
| 6 | **Planet screens** (land → hangar / equipment / shop / government / news tabs) | **MISSING as a UI surface** — all the underlying services exist as terminal commands + a compact market side-panel: `market/buy/sell/refuel` (shop), `outfit/install/weapon/upgrade` (equipment), `hull/repair/blackmarket` at base (hangar), `contracts/rank/rep` (government), NEWS ticker events. No dock screen with tabs. | 4 |
| 7 | **Text quests** (interactive-fiction missions from governments; SR2's signature) | **HAVE-partial (engine landed 2026-07-07, SR-M9)** — `textquests.js`: branching node-graph quests, 3 templates (10-12 nodes each), every named entity grounded in the LIVE galaxy (real planet/pilot, not fabricated), effects apply via the same credits/score/reputation pathway `missions.js` uses. Offered on dock; playable via the QUESTS tab or `quest`/`choose <n>`. MISSING vs SR2 still: only 3 templates (SR2 has hundreds), no skill-check rolls, no multi-dock quest chains. See `USE_CASES.md` UC-61. | 5 |
| 8 | **Ground battles** (SR2: robot-RTS planetary battles) | **HAVE-partial (deliberate analog)** — AWAY module (line ~2810): `land` → walk a 3D settlement on foot → hostiles hunt you → **turn-based 8×8 grid tactics on the REAL Tami rules** (C# sidecar :7877 > WASM C# > legacy JS tiers). Turn-based instead of RTS by design (Tami tie-in). MISSING: objectives beyond skirmish (capture/defend), terrain, deployment. | 5 |
| 9 | **Salvage** (loot floats after kills: goods, equipment, protoplasm/nodes) | **HAVE** — death drops full credits as a persistent stash; cargo pods eject + salvage; derelict encounters (40% artifact); flyover pickup; asteroid mining → gems. | 4 |
| 10 | **Artifacts** (unique passive-power items) | **HAVE-partial** — 5 artifacts w/ passives (M6), tier-scaled drops from Hegemon wrecks, `artifacts` list/equip. MISSING: identification/analysis, set effects, larger roster, quest-sourced uniques. | 4 |
| 11 | **Reputation** (per-race/per-government standing; criminal record; medals) | **HAVE** — per-planet rep spreading across system factions; gates prices + docking; kills/liberations raise, bombing tanks (M2); customs record via contraband scans; `rep`/`standing` readout. MISSING: medals/decorations, per-faction storyline consequences. | 5 |
| 12 | **Dynamic news** (galaxy feed reporting real events: invasions, prices, ranger deeds) | **HAVE-partial** — NEWS shocks (shortage/glut, stock-only so prices stay organic, M11) + event ticker + war banners. MISSING: a browsable, persistent news TIMELINE derived from the live war/society state (invasions, liberations, deaths, epoch die-offs). | 4 |
| 13 | **Ship equipment slots** (hull w/ slot/weight budget, 5 weapon mounts, engine/tank/radar/scanner/droid/grippers; micromodules) | **HAVE (2026-07-08, was stale - this row predates SR-M12 + the SHIP LAYOUT pass)** — 7 hull classes (M9) each with a REAL per-hull mount schema (`HULL_MOUNTS`, not a role-bucket), 4 weapon types incl. Hegemon-only flame (M3 + M6), single/multi-slot fits for engine/fuel-tank/radar/scanner/repair-droid/shield-generator/cargo-hook, a 2-6-slot gizmo bay + 1-5 weapon hardpoints (varies per hull, bigger hulls carry more AND wider firing arcs), hull series ("Acrynic" specializations) + manufacturers + tech-level susceptibility, 2 micromodules, mount/unmount/sell for every slot type. Visual layer: `engbay.js` renders the docked ship's REAL mount positions as a top-down clickable schematic (`engbay`/`loadout`/`E` key/HANGAR tab button) - click empty to mount, filled to unmount, same underlying commands as the text UI. MISSING vs a full weight-budget system: no per-item WEIGHT/mass cost (SR2's Tetris-like "bigger items cost more of a shared grid budget," as opposed to this game's flat per-type slot counts) - a real, still-open gap if that granularity is wanted later. | 5 |
| 14 | **Stations** (science/medical/business/military/pirate/ranger bases as distinct dockables) | **HAVE-partial** — one Ranger Command star base (M8: repair/hull-swap/black-market). MISSING: the other station classes (science = artifact analysis, military = joint ops, pirate = criminal lane). | 3 |
| 15 | **Protoplasm / Dominator evolution economy** (invader drops as currency; the enemy adapts via its own "program" updates) | **MISSING** — artifacts drop, but no Hegemon-specific salvage currency and **no enemy adaptation across campaigns**. Our unique angle: reuse the bench's genetic machinery (mutation + selection, N6) so the Hegemon EVOLVES against the player's observed tactics. | 5 |
| 16 | **Save / persistent galaxy** (a living galaxy you leave and return to) | **MISSING for game state** — knowledge store + Inhabitant state DO persist (localStorage autosave, `inhabitant` readout), but credits/rank/rep/war/campaign/epoch reset on every reload. Blocks generations (N6) and any "my galaxy" attachment. | 5 |
| 17 | **Player skills** (Accuracy/Maneuverability/Tech/Trade/Charisma/Leadership grown over a career) | **MISSING** — rank gates gear but no skill points; AI pilots DO learn (nav gain, ray weights, discovery) — the player has no equivalent progression. | 3 |
| 18 | **Black-hole arcade battles** (minigame → artifact rewards) | **MISSING** — and a poor fit; our real-space combat + away battles already cover "risk pocket → reward". Not planned. | 1 |
| 19 | **Pirate career lane** (be the criminal: raids, bribes, amnesty) | **HAVE-partial** — contraband buy/fence, customs fines, `bombard` with rep consequences make hostility *possible*; no career support (pirate rep lane, bribes, amnesty, pirate base). | 3 |
| 20 | **Time model** (real-time-with-pause, day granularity, aging galaxy) | **HAVE-partial (different)** — continuous real-time sim, no pause-and-order layer, no calendar. Epoch clock (N6) will be our calendar analog. | 2 |

---

# 20 MILESTONES

Sizes: S (≤half a session) · M (a session) · L (multi-session / a wave lane).
**SR-M1..SR-M8 = THIS WAVE (LANDING).** Acceptance criteria are the measurable gate — no green, no landed.

## THIS WAVE — LANDING

- **SR-M1 · Starmap overlay + territory + generation-persist ✅ LANDED 2026-07-08** — the starmap overlay (`starmap.js`, 685 lines, canvas-rendered systems shaded by owner + tooltips + generation tally) turned out to ALREADY exist and be substantial - this pass added the missing SAVE/LOAD half (`gatherSaveState`/`applySaveState`/`saveGame`/`loadGame` in `starfighter.html`, `SF_SAVE_v1` key, autosaves every 15s + on `beforeunload` + immediately after a generation reset, restores at boot over the fresh-boot defaults) and wired the new `lawless` system designation into `ownerOf()`/`OWNER_COL` (was falling through to a plain "coalition" read, now its own 4th state/color).
  *Accept:* ✅ verified live across a REAL page reload (not simulated) - set credits/score/hullClass/weaponType/a specific planet's rep/a specific system's contested flag/escalation/warScore/campaign to distinctive values, saved, reloaded the actual page, every single field matched exactly including the hull's visual model; ✅ `STARMAP.territory()`/`ownerOf()` correctly report the lawless system as its own category, not folded into coalition. **Size L · depends: —**
- **SR-M2 · Ground capture-the-flag + defenses + infra ✅ LANDED 2026-07-08 (already existed, one real gap fixed)** — found `conquest.js` (442 lines, own 10/10 test suite): `onAwayVictory(p)` flips a world to the player, `tick(dt)` runs Synod counter-invasions to take it back, `addDefense(p)` buys defense levels, full 4X infra damage/regen/production-multiplier - essentially SR-M2's entire spec, already built and wired into `planetmenu.js`. The one real gap: capture moved REP correctly but never touched `warScore` at all - that only reacts to system-wide contested/cleared status (enemy SHIP presence via `warTick()`), a completely separate axis from ground captures, so a player could capture many worlds with zero war-front movement. Fixed: `awayVictoryHooks()` now bumps `warScore` by `CFG.WARSCORE_PER_CAPTURE` (0.35, smaller than a whole system clearing's flat +1) whenever the captured world was ACTUALLY hegemon-held before the win - gated so "capturing" an already-friendly world (not a real liberation) can't farm war-front progress.
  *Accept:* ✅ verified live - `CONQUEST.onAwayVictory` correctly flips hegemon/rep (owner reads back 'player' via `CONQUEST.ownerOf`, matching "a second landing shows the held" state); the new warScore bump fires exactly once per genuine capture (-2.3 → -1.95, matching the constant precisely) and is confirmed a no-op against an already-friendly world; survives reload via SR-M1's save (planets already serialize `hegemon`/`rep`). **Size L · depends: SR-M1**
- **SR-M3 · Rank-gated ranger missions ✅ ALREADY LANDED (found complete 2026-07-08, no new code needed)** — `missions.js` already does exactly this: the board mixes unlocked + some locked postings (`ensureBoard()`), `rowHtml()` dims locked rows and shows `[RANK N - name]` + a red "- requires <rank>" note, `accept()` refuses with the rank named.
  *Accept:* ✅ verified live - at Recruit, `MISSIONS.accept()` on a locked slot returns `{ok:false, msg:'requires Ranger rank (you are Recruit)'}`; after `P.score=1000` the SAME slot returns `{ok:true, msg:'accepted: ...'}`. **Size S · depends: —**
  ⚠ **CORRECTION 2026-07-08 (user report "the mission in the menu don't work when landed at the planet")**: the verification above called `MISSIONS.accept()` directly, which is genuinely correct - but it bypassed the exact UI wiring the player actually clicks, which had a real bug: `planetmenu.js`'s docked MISSIONS tab falls back to generic "ACCEPT 1/2/3/4" buttons (since `MISSIONS` never exports a `list()` fn, so the nicer per-row button path is permanently dead code) using a 0-based `data-i`, but `MISSIONS.accept(idx)` expects a 1-based index (`i=(idx|0)-1` internally) - so EVERY click was off by one slot, silently rejected as "no such posting," and `tryAccept()` discarded the return value entirely so even that rejection was invisible. Fixed both: `data-i` now sends `j+1`, and `tryAccept()` now routes `accept()`'s `{ok,msg}` through `notifyHost()` so any future rejection (rank too low, already on assignment) is visible instead of silently doing nothing. LESSON: **testing a module's function directly is not the same as testing the click path that's wired to it** - this exact gap (verify the underlying function, never actually click the button) is what let this ship for a full session undetected.
- **SR-M4 · SR planet screen ✅ LANDED 2026-07-08 (already existed under different tab names, 3 real gaps closed)** — `planetmenu.js` already IS this: a full-screen docked overlay with 7 tabs (MARKET/HANGAR/MISSIONS/QUESTS/GROUND/LOG/DEPART) covering HANGAR+EQUIPMENT (loadout slots), MARKET (SR-M7's table), GOVERNMENT (missions+quests+rep), NEWS (the LOG tab) - just organized differently than the spec's exact 5 names, and Esc/DEPART already returns to space. The doc's own audit had this rated MISSING as a UI surface; it wasn't. Found + closed the 3 gaps its own hint text admitted to ("blackmarket and fence are still terminal-only" / "terraform is still terminal-only"): added a TERRAFORM row to the MARKET tab, and a BLACK MARKET panel (buy at Ranger Command or a pirate station, fence at a hostile dock) to the HANGAR tab - both the same `data-cmd`→`HOST.runCmd` path everything else here uses.
  *Accept:* ✅ verified live through the real clickable UI (not the terminal) - terraform's ADVANCE button moves `p.terra` exactly like typing it; blackmarket's BUY button and fence's SELL ALL button both move credits/contraband identically to their terminal equivalents; 0 console errors across the whole sequence. **Size L · depends: SR-M7**
- **SR-M5 · Real FBX ship/asteroid/gem models ✅ LANDED 2026-07-08 (found ~90% already built, one real gap closed)** — audit found `loadGameAssets()` already loads real FBX for all three asset types (3 ship files, 6 asteroid files, 3 gem files, all present in `assets/`), applies per-team materials (`fixShipMaterial`), and hot-swaps every ship ALREADY FLYING the instant its 3 files resolve (`shipDone`). The one real gap: asteroids/gems had NO equivalent retrofit - `rockMesh()`/`spawnGem()` only ever consulted `ASSETS.astGeos`/`gemGeos` at construction time, so any rock/gem already in the scene before the FBX geometry finished loading stayed on its procedural fallback forever (only newly-spawned ones after that point got the real shape). Fixed by mirroring the exact `shipDone` pattern: `astDone`/`gemDone` counters + callbacks that, once every file in the group has settled, swap ONLY the `.geometry` (disposing the old one) on every existing asteroid/gem - artifact pods and micro-module drops are explicitly excluded from the gem retrofit since they build their own distinct geometry directly, never through `spawnGem`'s ASSETS check. No LOD/instancing added (audit found none existed; not revisited this pass - SR-M8's adjacent perf pass already confirmed frame budget is fine with the existing clone-once strategy).
  *Accept:* ✅ (pre-existing, reconfirmed) 0 console errors, ships/asteroids/gems visibly FBX-rendered, asset requests observable; ✅ (new) the retrofit path syntax-checked and live-loaded cleanly - not independently re-measured for fps this pass (no code touched the render/physics hot path). **Size M · depends: —**
- **SR-M6 · Unique per-weapon projectiles ✅ LANDED 2026-07-08 (2 of 4 weapon shapes + all impact FX were the real gap)** — audit found energy (`bolt`: elongated light-blade + halo) and flame (`flame`: real fire-sprite gout) already fully distinct, muzzle flash already per-weapon-colored, and homing already real (missiles steer + a smoke trail, gated on `homing>0`). The actual gap: ballistic (`tracer`) had NO real branch at all - it fell through to the exact same round glow-sprite as missile (`orb`), just recolored, so an Autocannon never looked like a streak. Added a genuine `tracer` branch reusing `BOLT_GEO` (cheap - no new geometry) scaled thin+long and oriented to the flight direction, no halo (a stark bright line, visually distinct from the laser's fat halo'd blade). Also found impact FX were keyed to the TARGET's own color/a hardcoded value at all 3 general impact sites (ship/asteroid/stronghold hits), not the weapon - so a laser, autocannon, and missile hit the same ship looked identical. Added `col` to the bullet's own persisted data record at spawn time and switched those 3 sites to use `b.col` (falling back to the old hardcoded value only if absent) - point-defense's missile-interception burst was deliberately left alone, that's its own distinct "intercepted" signal already gated to missiles only, not a case of lost per-weapon variety. Grep-confirmed no other impact site touches bullet color.
  *Accept:* ✅ all 4 shapes (bolt/tracer/orb/flame) now genuinely visually distinct in code (not verified via an actual blind screenshot test this pass - the code-level distinction was verified directly instead: 4 different mesh/material constructions, no shared branch); ✅ homing trail confirmed pre-existing and untouched; ✅ per-type impact FX now real - `b.col` traced end-to-end from `WEAPONS[type].col` through to the burst call, syntax-checked. **Size S · depends: SR-M5**
- **SR-M7 · Market table view ✅ LANDED 2026-07-08** — found `planetmenu.js`'s docked MARKET tab already had the interactive trade table half (rows=goods, buy/sell price, stock, your cargo/HOLD, click-to-trade with quantity steps, profitable-sell highlight) - it uses the exact same `data-cmd="buy/sell ..."` → `HOST.runCmd` path the terminal does, so identical-results was already guaranteed by construction, not something to rebuild. The real gap was the OTHER surface, the top-center overview grid: it called `priceOf()` live for every planet unconditionally, an omniscient read violating this project's own no-cheat economy law. Added `recordObservedPrices(p)` (a per-planet `{good: {price, atT}}` snapshot, taken at the moment of docking and continuously refreshed only for wherever you're CURRENTLY docked); the overview grid now shows a live read only for that one planet, your last personal snapshot (dimmed/italic) for anywhere you've visited before, and "?" for anywhere you never have.
  *Accept:* ✅ click-trades already identical to terminal buy/sell (shared code path, not two implementations); ✅ verified live - an unvisited planet's cell shows "?"; docking shows the live price; buying elsewhere (simulating other traffic) while away does NOT leak into your cell - it stays frozen at your last personal observation until you dock there again. **Size M · depends: —**
- **SR-M8 · 50-AI ramp ✅ LANDED 2026-07-08 (already existed, verification-only)** — every piece this milestone asks for turned out to already be built by an earlier "N7" pass that had explicitly deferred just the live-scale verification: `novel/characters.json` already carries the full 50-name roster (`NOVEL ROSTER loaded: 50 characters`), `CFG.THINK_STRIDE=4` already shards AI brains across frames (physics every frame, decisions every 4th, comment literally says "50-mind ramp"), the ship-vs-ship collision loop already has an axis-aligned broadphase early-reject, and `chatter.js` already rate-caps both the ambient chatter job queue and the speechSynthesis queue (`VOICE_CFG.QUEUE_MAX=6`, drop-never-backlog). Nothing was missing - this pass was pure measurement.
  *Accept:* ✅ verified live via a wall-clock-independent driver (real backgrounded preview tabs fully suspend `requestAnimationFrame`, so a genuine 5-minute wait doesn't work here - instead replicated the exact real per-frame call sequence, incl. all 3 `renderAll()` viewport renders, in a tight synchronous loop timed with `performance.now()`) across a 340-simulated-second fresh-boot run (20423 frames): population ramped 32→57 alive, held a continuous 55-57 plateau for 60+s, then oscillated high-40s/high-50s (mean 47.5) as the existing war-escalation economy (SR-M2/M7) organically cut pirate spawn caps on each front liberation - a real emergent rhythm, not a bug; 98.33% of frames rendered under 16ms (bins: <8ms 17152, 8-16ms 2929, 16-24ms 268, 24-33ms 60, 33-50ms 10, 50+ms 4); with voices toggled on mid-run the speechSynthesis queue filled to and held exactly at `QUEUE_MAX=6` under real load (not a vacuous always-0 pass) while the ambient queue stayed ≤4; 0 console errors across the entire run including the voice-synthesis stretch. **Size L · depends: —**

## FUTURE

- **SR-M9 · Text-quest engine ✅ LANDED 2026-07-07** — shipped as `textquests.js`, offered on dock (any planet, not gated behind a GOVERNMENT tab since SR-M4's full 5-tab rewrite hasn't landed) rather than novel-pipeline-authored: every named entity is grounded in the LIVE `window.HOST` galaxy at offer time (a real planet/pilot that exists right now) instead of `novel_graph.json` canon-auditing, which is arguably a STRONGER grounding guarantee (verifiable against the live sim, not just static lore consistency) but doesn't reuse the novel/chapter assets.
  *Accept:* ✅ 3 completable quests of 10-12 nodes each (exceeds the ≥8 floor); ✅ grounding verified two ways - the module's own self-test (16/16, `node textquests.js`) walks every template's full graph and confirms offered text names a real stubbed entity, AND a live `preview_eval` dock produced a real offer naming an actual in-game planet; ✅ rewards pay via the exact pathway `missions.js` already uses (`p.credits`/`p.score`/`HOST.repAdd`). Remaining gap vs the original accept bar: not novel-pipeline-authored, not GOVERNMENT-tab-gated (added as its own QUESTS tab instead - SR-M4 doesn't exist yet to gate it behind). **Size L · depended on: SR-M4 (worked around instead - see `USE_CASES.md` UC-61)**
- **SR-M10 · Government + news from live war state ✅ LANDED 2026-07-08 (GOVERNMENT half already existed, NEWS half had a real gap, both closed)** — audited both halves before writing anything. GOVERNMENT: `missions.js` already generates mission content directly from live war/faction state (`genLiberate` only ever targets planets actually `p.hegemon===true`, `genPatrol`/`genSupply` bias toward contested/`underThreat` systems, `genAssault`'s text lists real contested front-line system names) - the MISSIONS tab already IS the spec's GOVERNMENT half, just under a different tab name (same pattern as SR-M4). NEWS: found a real, clean gap - every war-front/economy event (system contested/liberated, Hegemon Stronghold rise/destroy, M11 economy shocks) called the bare `ev()` helper directly instead of the `notify()` wrapper, so none of them ever reached the docked planet-screen's LOG tab (only `notify()`-level stuff did) even though they were exactly the "invasions, liberations, shocks" categories the milestone names. Fixed at the single choke point instead of patching every call site: `ev()` itself now also calls `PLANETMENU.pushEvent` (and the now-redundant duplicate call inside `notify()` was removed, since `notify()` already calls `ev()` internally) - every one of the 13 existing `ev()` call sites across the game automatically reaches the NEWS tab with no further wiring. Also bumped `LOG_MAX` 12→80 (a real browsable timeline, not a 12-line scratchpad), renamed the tab label LOG→NEWS, and added `PLANETMENU.getLog()/setLog()` threaded through `gatherSaveState`/`applySaveState` so the timeline is genuinely persistent across a reload (not just "browsable" within one session). Honest scope note: "ranger deaths" and "epoch die-offs" are NOT implemented as real event categories anywhere in the shipped game (that's the separate, not-yet-wired `NOVEL_SOCIETY.md` N6 evolution/die-off pillar) - did not fabricate placeholder events for either; the NEWS tab only ever shows genuinely-logged events, same 0-fab standard as everything else in this project.
  *Accept:* ✅ verified live - firing the actual `ev()` call used by the real system-contested code path immediately appeared as the top entry in `PLANETMENU.getLog()` AND rendered correctly through the real clickable NEWS tab (native click → tab body shows the exact event text + timestamp); save/load round-trip confirmed (`gatherSaveState().newsLog` → clear → `applySaveState` → identical restore); mission-board reactivity confirmed over 15 forced regenerations of a planet flipped to `hegemon=true` - 4/15 produced a fresh LIBERATE mission naming that exact planet (consistent with random competition for the board's 4 visible slots against ~9 other candidate generators per cycle, not a wiring failure - the underlying data is always read fresh every cycle, inclusion on any single cycle just isn't guaranteed by a small rotating job board, same as the genre reference). 0 console errors throughout. **Size M · depends: SR-M1, SR-M4**
- **SR-M11 · Planetary battles upgrade (RTS analog → Tami tactics campaign)** — bigger away maps (≥12×12), terrain tiles with movement cost/blocking/height (the Tami bring-over below), a deployment phase (pick party from a roster), and liberation OPERATIONS = 2–3 linked battles that flip a Hegemon-held world.
  *Accept:* one full liberation op playable; terrain measurably alters reachable tiles; deployment choice changes the fielded party. **Size L · depends: SR-M2**
- **SR-M12 · Equipment slots + artifact expansion ✅ LANDED 2026-07-08 (merged from a concurrent session's PR, verified + reconciled into source this session)** — real slot-based loadout: single-slot ENGINES (Standard/Afterburner/Ion), a 2-slot GIZMOS bay (Targeting Computer/ECM/Shield Booster), hull ROLE (Fighter/Merchant/Mercenary/Corsair) driving gizmo-slot *and* weapon-HARDPOINT count, E/S/M damage-type susceptibility + point-defense per hull, Fuel Tank/Radar/Repair Droid single-slot swaps, new Scanner/Shield Generator/Cargo Hook slots, micro-modules (Optimator/Integrator, drop from Warlord-tier+ kills, gated behind owning a Cargo Hook), player-only missile ammo magazine, HULL SERIES (SR "Acrynic": Fast/Dandy/Repair/Ironclad/Pygamore, single-slot fit), MANUFACTURERS (Human/Peleng/Maloq/Faeyan/Gaalian, chosen at hull purchase), and TECH LEVEL riding the existing `upgrade hull` progression (-4%/level E/S/M susceptibility, floored, Lv1 = exact no-op). Deliberately did NOT build a full Manufacturer×TechLevel×Series procedural hull *generator* (no assets exist for hulls beyond the 7 hand-authored models - would need placeholder visuals or dishonest reuse) - built the mechanical grammar instead, composed onto the existing 7 hulls. Multi-weapon hardpoints are player-only (AI keeps its existing single-weapon fire loop unchanged - a shared per-frame AI tick running dozens of ships made independent multi-weapon fire loops a real perf/balance risk for zero player-facing benefit). New terminal commands: `tank/radar/scanner/shieldgen/droid/hook/series` (fit), `hardpoint mount|unmount`, `ammo` (restock), `probe` (scan a target's cargo/equipment, blocked by their Scanner vs shield-derived %), `micromods`/`mm`. `planetmenu.js` HANGAR tab renders every new slot type via the same generic single-slot / mount-unmount-sell UI pattern GIZMOS already used. Save/load persists every new field; HOST bridge exposes all new tables.
  *Accept:* ✅ headless-verified per the PR's own test plan (fit/swap every gear system, hull+manufacturer purchase, hardpoint mount/unmount/sell with cap enforcement + 50% refund, series fit, gizmo/hardpoint-bay resize+refund on a role-changing hull swap both directions, tech-level susceptibility scaling incl. Lv1 no-op + Lv6 -20% + floor, missile ammo consumption/restock, save/load roundtrip, HANGAR UI transacts via real clicks) across 7 commits, 0 new console errors; ✅ this session additionally confirmed live post-merge: page loads clean, `HOST.hardpointCountFor` present, 0 console errors with this session's own panel-system changes layered on top. Outstanding per the PR's own honest gap: no manual full-session "does it feel coherent in practice" playtest yet. **Size M · depends: SR-M4**
  **⭐ FOLLOW-UP 2026-07-08 "SHIP LAYOUT" (the user's original ask - a real top-down mount-point builder, not just abstract slot counts) — LANDED:** M12 above gave every hull real gizmo/hardpoint slot COUNTS but no POSITIONS - `hullmodels.js` (the per-class visual silhouette system) confirmed to have zero mount-point data anywhere, and every hardpoint weapon fired dead-ahead regardless of which slot it was in. Added `HULL_MOUNTS` - real per-hull mount positions + facing angles, replacing the old `GIZMO_SLOTS_BY_ROLE` bucket lookup (which had given cruiser/dreadnought/capital IDENTICAL counts to interceptor, since all four shared `role:'fighter'` - a real gap now fixed: scout/fighter/interceptor/freighter keep their exact prior counts, cruiser/dreadnought/capital gain real capacity matching their size/cost/speed). Interceptor/cruiser/dreadnought/capital's outer weapon mounts are now genuinely angled outward (12°/20°/30°/35°) rather than all dead-ahead, so bigger hulls get a real wider firing ARC, not just more damage. New `engbay.js` module renders the docked ship's real mounts as a clickable top-down SVG schematic (`engbay`/`loadout` command, `E` key, or a HANGAR tab button) - click empty to mount, filled to unmount, routing through the EXACT same `gizmo`/`hardpoint mount|unmount` commands the text UI already used (no new mutation path). Wired hardpoint weapons to actually fire along their OWN mount's facing angle instead of the ship's raw nose - a target sitting exactly on an angled mount's firing line now correctly triggers that weapon even when the ship's nose isn't pointed at it.
  *Accept:* ✅ mount-count derivation verified live for all 7 hulls against the intended design (4 unchanged, 3 increased) with HULL_SERIES deltas still composing correctly on top; ✅ the full click-to-mount/unmount cycle verified via REAL DOM clicks on the actual SVG (not direct function calls) - empty mount → picker → pick → mounted + credits deducted exactly → click filled → unmounted; all three discovery paths (terminal command, hotkey, HANGAR button) verified via real command dispatch/keydown event/button click; dock-gating verified (shows a message instead of the schematic when undocked); `engbay.js`'s own 15-check node self-test passes. ✅ the weapon-facing geometry verified EXACTLY via direct calculation (a target placed on a -35° mount's firing line: new per-mount dot = 1.000000 vs the old ship-nose-only dot = 0.819152 = cos(35°) precisely, correctly below both fire thresholds) - the full AI-targeting integration (mode/target selection) is pre-existing/unchanged code and wasn't independently re-driven end-to-end in this pass (an ad-hoc synthetic-hostile test didn't get the player's `think()` into HUNT mode for unrelated pre-existing reasons, not a fault in the geometry change itself). MISSING: no free-form circle/arc PLACEMENT (mount positions are fixed per-hull, not player-repositionable) and no shield-coverage visualization on the schematic - both deferred, not silently dropped.
- **SR-M13 · Dynamic economy shocks 2.0 (war-coupled) ✅ LANDED 2026-07-08 (blockades already existed, 3 gaps closed)** — audit found `econTick`'s existing `CFG.CONTESTED_PROD:0.4` was ALREADY a real "blockades on contested lanes" (contested-system worlds already produce at 40%) - not previously credited as satisfying part of this milestone. The 3 genuinely missing pieces, added as extensions of the SAME stock/flow multiplier `econTick` already uses (never a price write): **refugee demand spike** - contested-system worlds now consume FOOD at `CFG.REFUGEE_DEMAND_MULT`×(1.8) the normal rate, since only food reflects displaced-population strain, not every good; **post-liberation boom** - the exact moment `warTick` flips a system from contested→liberated now sets `p.boomT=CFG.LIBERATION_BOOM_DURATION` (25s) on every planet in that system, consumed by `econTick` as a `CFG.LIBERATION_BOOM_MULT`×(1.6) production surge that counts down and expires on its own; **embargoes at hostile-rep worlds** - any planet with `rep<=CFG.REP_HOSTILE` now produces at `CFG.EMBARGO_PROD`×(0.55) regardless of whether its system is actively contested (reputation-based isolation, independent of active invasion - the two stack multiplicatively for a Hegemon-held world, which is both contested AND hostile-rep by construction, an intentionally steep combined penalty for an occupied+isolated world).
  *Accept:* ✅ grep-level audit confirms zero direct price writes anywhere in the file (prices are always `priceOf()`-derived from stock); ✅ all 3 new multipliers verified live against EXACT expected ratios via a real `econTick(dt)` call on a live planet (blockade 0.4, embargo 0.55, boom 1.6, refugee-demand 1.8 - all matched to 3 decimal places) rather than eyeballed; ✅ the liberation hook verified via a real `warTick()` call - forcing a system's grace-period to expire correctly set `boomT` to the exact configured duration on every one of its real planets. Not built this pass: the milestone's own "logged A/B vs a control campaign" comparison (the mechanism is real and measured in isolation; a full multi-campaign A/B trend log was not run). **Size M · depends: SR-M10**
- **SR-M14 · Ranger leaderboard across generations ✅ LANDED 2026-07-08 (merged from a concurrent session's PR; one real gap closed this session)** — a persistent, reload-surviving, cross-generation leaderboard already existed (`recordGenerationResults`/`SF_LEADERBOARD_v1`, fired once per real epoch from `starmap.js`'s `beginNextGeneration`), tracking score/wealth/rank for every living squad pilot including the player, printed via `leaderboard`/`hof`/`rankings`. Audit found it was missing the milestone's OTHER named ranking axis: thought-fitness (the N5 ledger in `thoughts.js`). Added: `recordGenerationResults` now also reads `THOUGHTS.fitness().pilots[name].fitness` (a one-shot snapshot taken once per generation-reset, not per-ship) and stores it per row; the `leaderboard` command now prints it inline when present, and gracefully omits it for any pre-existing row saved before this fix (no fabricated backfill). NOT built this pass: a dedicated lineage VIEW (N6) - N6 itself remains entirely unwired into the live game (confirmed still true, see SR-M20 below), so a lineage view has nothing real to display yet; this is a genuine remaining gap, not silently dropped.
  *Accept:* ✅ (pre-existing) leaderboard survives reload + a real generation boundary, player ranked among AI pilots; ✅ (new) thought-fitness field verified present/absent correctly (old rows show none, confirmed via the loose-equality `!=null` check against `undefined`). Remaining gap: no lineage view. **Size M · depends: SR-M1, SR-M8**
- **SR-M15 · Hegemon evolution (Dominator/protoplasm analog)** — Hegemon wrecks drop CORES (invader salvage currency spent at base/science); the Hegemon ADAPTS across campaigns via the bench's genetic machinery — mutation + selection on its loadout/tactic parameters against observed player behavior.
  *Accept:* after 3 campaigns the Hegemon loadout distribution measurably shifts vs the player's dominant weapon; a no-selection control campaign shows no shift (the N6 audit pattern). **Size L · depends: SR-M14**
- **SR-M16 · Multi-system galaxy + jump lanes** — grow 3 → 8+ systems as separated bubbles joined by jump gates/hyperlanes (fuel-gated fast travel — SR jumps reintroduced without breaking the balance reason they were removed); starmap shows lanes; war spreads along them.
  *Accept:* a jump consumes fuel + transit time and is lane-constrained; a contested system infects lane-adjacent systems first (logged spread). **Size L · depends: SR-M1**
- **SR-M17 · Pilot skill career ✅ LANDED 2026-07-08 (genuinely new - confirmed absent before building)** — unlike every other item this wave, a grep for any skill-point concept came back empty, so this one was built fresh rather than found. 6 skills (gunnery/handling/tech/trade/charisma/leadership), 0-5 levels each, funded by 1 point per rank gained (`checkRankUp` now computes the rank delta before overwriting `rankShown` and banks `CFG.SKILL_PTS_PER_RANK` per level gained). Player spends via `skills`/`skill <name>` terminal commands; AI pilots auto-allocate on their own rank-ups via `autoAllocateSkill(s)`, weighted by `SKILL_WEIGHTS[s.role]` so temperament genuinely diverges instead of every pilot converging on the same build. Each skill hooks into exactly ONE existing formula (deliberately, so the effect is a single-line, easy-to-audit multiplier, not a new subsystem): gunnery → weapon damage, handling → thrust force, tech → shield regen rate, trade → buy/sell price (the milestone's own named example), charisma → customs-scan fine reduction, leadership → mission reward multiplier (exposed via a new `HOST.skillLvl(s,k)` bridge fn so `missions.js` can price it into `mkMission()`'s reward at generation time). Threaded `s.skills`/`s.skillPts` through `gatherSaveState`/`applySaveState` (SR-M1) so a skill build survives a reload.
  *Accept:* ✅ verified live, all 6 - a rank-up (`rankShown` 4→5) correctly banked 1 point; gunnery Lv5 → weapon dmg ×1.20 (18→21.6, exact match to `1+0.04·5`); handling Lv5 → thrust ×1.15 (exact); tech Lv5 → shield regen ×1.30 (exact); trade Lv5 → price ×0.90 both via the raw pre-rounding formula (exact) AND a real `buy()` call (51c→49c, matched once qty-rounding noise at tiny quantities was isolated out); charisma Lv5 → customs fine ×0.40 via a REAL `scanBust()` call (540c→216c, exact); leadership Lv5 → mission reward ×1.25 via the actual `HOST.skillLvl` bridge `missions.js` calls (exact). AI temperament divergence confirmed strongly, not just nominally: 40 trials of 6 points each on a synthetic trader vs raider ship produced near-inverse profiles (raider: gunnery 90/handling 61 highest, trade 11 lowest; trader: trade 92/charisma 64 highest, gunnery 6 lowest) - a large point budget (30+) was tried first and showed no divergence at all (everything maxes out regardless of weighting once points exceed 6 skills×5 levels), which was itself a useful reminder to test at a budget where the ceiling doesn't erase the signal. Save/load round-trip confirmed exact (`{gunnery:3,trade:2}`, 4 pts → save → clear → restore → identical). 0 console errors throughout. **Size M · depends: SR-M3**
- **SR-M18 · Bars, rumors + wingmen** — planet-screen BAR tab where docked AI pilots hang out and talk (novel-grounded); rumors are verified pointers to real live state (signals, contracts, war intel — 0-fab); hire a wingman who follows and fights.
  *Accept:* 20/20 rumor claims verify against live state; a hired wingman escorts + engages and costs upkeep. **Size M · depends: SR-M4, SR-M8**
- **SR-M19 · Science stations + probes** — a science base that IDENTIFIES artifacts (unidentified until analyzed) and upgrades them for CORES; planetary probes that yield minerals over time; both persist.
  *Accept:* an unidentified artifact refuses to equip until analyzed; a deployed probe pays out across reloads. **Size M · depends: SR-M12, SR-M1**
- **SR-M20 · Campaign scoring + New Game+ ⚠ HALF LANDED 2026-07-08 (scoring built + verified; NG+/lineage explicitly deferred, not faked)** — confirmed genuinely untouched before starting: `warWin`/`warLose` only ever moved credits/score with no breakdown, `fireGameOver` (starmap.js) shows only the generation number + knowledge-fact counts, and `evolution.js` - a COMPLETE, sophisticated N6 pillar (epoch-based fitness-proportional selection + gaussian-mutated offspring genomes + a no-selection control mode for scientific rigor + full persistence, its own file, its own CLI seed-run mode) - exists but is not even loaded via a `<script src>` tag anywhere; genuinely disconnected, not partially wired. Built the SCORING half: a documented formula, `CAMPAIGN_SCORE = |warScore|·W_WAR + credits·W_WEALTH + rankTier·W_RANK + missionsCompleted·W_MISSION + questsCompleted·W_QUEST + discoveries·W_DISCOVERY` (named weights in CFG, zero magic numbers in `campaignScore()` itself), rendered as a real breakdown line in the terminal (not just the existing fading victory/defeat banner) at both `warWin` and `warLose`. Two of the five components (missions/quests/discoveries) had no counter anywhere - added `p.missionsCompleted`/`p.questsCompleted`/`p.discoveries`, incremented at the ONE real completion choke-point each already has (`missions.js`'s `complete()`, `textquests.js`'s `choose()` terminal-node resolution, `resolveEncounter()`), rather than inventing a parallel tracking system. All three threaded through save/load. **Deliberately did NOT wire evolution.js's full continuous epoch-tick/die-off cycle into live gameplay** - its own `EPOCH_S:240` cadence would start permanently culling squadron pilots (real permadeath, bottom-K selection) throughout ordinary play, a much bigger and more disruptive change than "NG+ inherits lineage" asks for, and not something to rush into live gameplay half-verified. Wiring evolution.js properly (matching its roster naming to this game's `novelShipHook` roster, hooking `epochNow()`/`population()` at the generation boundary instead of continuously, mapping its mutated `{aggr, chatterTemperament, risk}` genome onto this game's actual AI-behavior fields) is real, scoped, follow-up work - flagged here rather than silently left off the burndown.
  *Accept:* ✅ formula documented (above + inline CFG comments); ✅ rendered live at campaign end - verified via a real `warWin()` call end-to-end (not just the isolated formula): breakdown line arithmetic matched by hand to the exact integer (240+95+180+100+20+20=655, `CAMPAIGN 1 SCORE: 655` printed correctly with the ENDED campaign number, not the incremented one); caught and fixed a real bug in the same pass - `rankOf(score).n` is the rank NAME string ('Legend'), the numeric tier is the separate `rankIdx(score)` function, and using the wrong one silently produced `NaN`→`null` until live-tested; ✅ all three new counters round-trip exactly through save/load. ❌ NG+ lineage inheritance not built (see above - explicitly deferred, not a silent gap). **Size S (scoring half only; a correct NG+/lineage half is realistically its own Size M given evolution.js's real integration surface, not S as originally estimated) · depends: SR-M14**

---

# TAMI 2D TOPDOWN BRING-OVER

Which Tami battle-presentation requirements apply to our AWAY battles, with honest current status.
Away module = `starfighter.html` lines ~2810–3235 (`AWAY` IIFE): ground walk → 8×8 turn-based grid;
battle math tiers: **C# sidecar :7877** (real TamiRules) > **WASM C#** > legacy JS stand-in.
Data: `tami_data.json` (baked by `bake_tami_data.js` — species/techniques/strikes/afflictions/18×18 matrix).

Caveat: `BattleRequirements.txt` is explicitly NOT gospel (see its Wave-47 notice) — authoritative pair is
`TamiRequirements.txt` + the GDrive sheet cache. Items below cite BattleReq lines as the *presentation* index.

- [ ] **Turn order UI** (R3.2 order-by-Agility, line 53; R9.3 upcoming-turns visible, line 129)
  — **HAVE-partial**: away sorts `order[]` by speed each round and marks the current unit `▶` in the roster panel (legacy `nextTurn` ~2952; core mode renders `st.current` ~3099). **MISSING**: a forecast strip of the NEXT N turns (R9.3) — the thing that makes Agility investment legible.
  Pointers: away UI `drawBattleUI` ~2999 / `drawCoreUI` ~3095; Tami reference = turn-order display in the battle HUD (GameManager.NextTurn drives it; REQ-AFFLICT-01 line 110 makes stun tick on EVERY unit-turn, so the strip must re-derive per turn, not per round).
- [ ] **Technique panel — flavour TOP + mechanics BOTTOM** (memory `project_tami_technique_panel_flavour`: TechniqueDB.Flavour.cs; TechniqueInfoPanel; desc auto-size 14–22 per the overflow fix)
  — **MISSING**: away technique buttons show only name/element/AP/range (`drawCoreUI` ~3108–3110; legacy TECHS table ~2908). No flavour line, no mechanics detail (power, hit rate, effect chance/duration, cooldown — ALL already baked per-technique in `tami_data.json`, see `bake_tami_data.js` ~109–118). **Gap in the bake: no flavour field** — the sheet gained a flavour column 2026-07-07; bake must add it before the panel can show flavour-top/mechanics-bottom.
- [ ] **Type-matchup display / combat preview** (R9.2 hit/crit/damage preview BEFORE acting, line 128; 18 elements + 4 immunities, lines 151–154 + 200–204)
  — **HAVE-partial**: matchup surfaces only POST-HOC in colored log lines (super effective/resisted/crit/miss, `coreLogLine` ~3083) and the demo driver picks by `TAMIDATA.matrix` internally (~3163). **MISSING**: pre-action preview on target hover (est. damage, hit %, crit %, effectiveness arrow) — the sidecar/wasm damage model + `hit_formula` in `tami_data.json` supply everything needed; immunities (0×) must render distinctly.
- [ ] **HP / affliction chips** (R8.2 afflictions incl. stacks-to-3, lines 100–108; REQ-AFFLICT-01/02 tick cadence + durations, 110–124; REQ-UI-04 single chip pool + “+N more” overflow, line 165; REQ-UI-CHIPROWS top-2/bottom-4 rows, line 181)
  — **HAVE-partial**: text HP bars + numbers per unit; legacy shows affliction initials, core mode shows name+turns inline (~3098–3099); afflictions actually tick (Burn/Poison/Stun, legacy ~2956; core = real rules). **MISSING**: chip-style per-unit surface (stack counts ×3, turns-remaining per chip, overflow “+N”), and any in-3D HP/affliction readout over units (only name labels float, ~2945).
- [ ] **Terrain tiles** (R2.3 type/cost/blocking/height, line 47; REQ-TILE-01 configurable highlight palettes, line 174; REQ-TILE-02 ridge visible only on flying-unit turns, line 175; sand-cost exemption, lines 215–216; movement types Aquatic/Flying/Spectral, line 149)
  — **MISSING**: away grid is a flat checkerboard (`buildBattle*` ~2936, ~3057) — no terrain types, costs, blocking, or height. **HAVE-partial** on highlights only: green move-range + red attack-range tile tinting (`paintTiles` ~2948, `corePaintTiles` ~3076) ≈ a fixed single palette of REQ-TILE-01.
- [ ] **Unit inspect surface** (REQ-UI-04 UnitInfoPanel single inspect surface, line 165; REQ-UI-07 sticky UnitCard, line 168; R9.1 inspect units/terrain/turn order, line 127)
  — **MISSING**: no hover/click inspect of a unit's stats/types/techniques in-battle; core state already carries level/types/AP/techs per unit (~3099).
- [ ] **AP resource display** (R7.2 AP start/max/regen, basic strike restores, line 93)
  — **HAVE (core tier)**: AP shown per unit + AP costs on tech buttons + strike shows +AP (~3099–3111). Legacy tier substitutes cooldowns (honest stand-in, labeled).
- [ ] **Deployment** (TAMI-SPECIFIC: 5 active from 8 roster, deployment tiles, line 148)
  — **MISSING**: fixed 3-unit party vs 2–3 foes (`party()`/`foeParty()` ~2920–2928; core roster from sidecar). Becomes SR-M11's deployment phase.
- [ ] **AOE techniques** (TAMI-SPECIFIC: inner auto-hit, perimeter dodges, line 150; R4.2.2)
  — **MISSING**: away battles are single-target only; `aoe` field is already baked per technique in `tami_data.json`.
- [ ] **Victory / defeat / unit replacement** (R10, lines 131–136)
  — **HAVE-partial**: victory/defeat/two-click retreat + credit spoils (~3007, ~3103, `retreatBattle` ~3029). **MISSING**: unit replacement from roster (R10.4), XP/bond post-battle (R10.5) — away rewards are credits-only.
- [ ] **Combat cinematic spacing** (REQ-CIN-01 distance-scaled combat screen, line 178)
  — **MISSING / probably N/A**: away resolves on-grid with no cutscene layer; only worth porting if we ever add a strike-zoom presentation. Lowest priority of this list.

---

*Maintenance: when a LANDING milestone ships, move its row's status in the burndown table (and keep the
acceptance evidence — screenshot/log path — next to the milestone line). This file is the SR-layer
counterpart of `SR2_MILESTONES.md` (space combat layer) and `NOVEL_SOCIETY.md` (society layer).*
